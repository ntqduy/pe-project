"""Independent pseudo-anatomy segmentation stage.

Concrete inference wrappers are imported from their modules so consumers of
saved mask artifacts never initialize segmentation inference implicitly.
"""
