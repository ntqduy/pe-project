# data.silver.rules_falcon — SL01: deterministic rules plus Falcon extraction.
#
# Question: does a rule-first cascade extract report supervision more reliably than an LLM alone?
# Support artifact; accepted rows only, never gold evaluation labels.
# Output: outputs/silver_label/SL_rules_falcon/
#   labels.parquet, audit.jsonl, result.json (same layout as the other silver runs)
_base_:
  - ../../../paths.yaml
  - ../../../compute/default.yaml
  - ../../../components/silver/rules_falcon.yaml
experiment:
  id: SL_rules_falcon
  name: data.silver.rules_falcon
  stage: silver
  family: silver
data:
  mode: full
seed: 42
