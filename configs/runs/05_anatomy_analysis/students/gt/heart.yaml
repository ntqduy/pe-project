# anatomy.heart_student — SUFFICIENCY: is the heart region alone enough to predict PE?
#
# Ground-truth supervision only, no teacher. Heart input comes from ROI2; the PA and lung
# branches still receive their stored masks, but the volume outside the heart ROI is erased.
# Pairs with anatomy.heart_student_kd (identical except for distillation).
# Output: outputs/roi_students/RS_heart_only_gt/
_base_:
  - ../../../../paths.yaml
  - ../../../../compute/default.yaml
  - ../../../../components/backbones.yaml#ct_fm
  - ../../../../components/encoders.yaml#from_c0
  - ../../../../components/tasks.yaml#diagnosis
  - ../../../../components/tasks.yaml#diagnosis_anatomy_reference
  - ../../../../components/training.yaml#diagnosis
  - ../../../../components/anatomy.yaml#organ_adapter
  - ../../../../components/fusions.yaml#concat_mlp
  - ../../../../components/tasks.yaml#roi_student
experiment:
  id: RS_heart_only_gt
  name: anatomy.heart_student
  stage: roi_student
  family: roi_student
task:
  input_counterfactual: keep_only_heart
